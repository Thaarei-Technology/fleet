{
  "name": "@thaarei/worker-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm --filter @thaarei/worker-app... build && node --import tsx --watch src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@thaarei/adapters": "workspace:*",
    "@thaarei/contracts": "workspace:*",
    "@thaarei/core": "workspace:*",
    "@thaarei/database": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "6.0.3"
  }
}
